# (O)pen (Acc)elerators
OpenACC applies to vector computing, which utilizes GPUs to parallelize operations across vectors.

### Use Case
When work is very independent and **MANY** things need to be done in parallel.
